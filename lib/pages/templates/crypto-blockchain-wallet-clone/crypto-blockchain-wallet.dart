import 'package:flutter/material.dart';

class CryptoBlockchainWallet extends StatelessWidget {
  const CryptoBlockchainWallet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
